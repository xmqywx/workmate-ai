services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: workmate
      POSTGRES_USER: workmate
      POSTGRES_PASSWORD: workmate_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
